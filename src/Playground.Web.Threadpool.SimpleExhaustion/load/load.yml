config:
  target: 'http://localhost:5000'
  phases:
    - duration: 60
      arrivalRate: 500
  defaults:
    headers:
      x-my-service-auth: 'not used'
scenarios:
  - flow:
    - get:
        url: "/exhaust"
  - flow:
    - get:
        url: "/healthy"